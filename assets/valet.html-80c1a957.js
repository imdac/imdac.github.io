import{_ as e,p as n,q as a,a3 as s}from"./framework-dd056dd6.js";const t={},i=s(`<h1 id="installing-laravel-valet" tabindex="-1"><a class="header-anchor" href="#installing-laravel-valet" aria-hidden="true">#</a> Installing Laravel Valet</h1><p><strong>1. Open a new Terminal window.</strong></p><p><strong>2. Enter the following command into the Terminal window and hit <code>return</code>.</strong></p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">composer</span> global require laravel/valet
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p><strong>3. Enter the following command into the Terminal window and hit <code>return</code>.</strong></p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>valet <span class="token function">install</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>You may be asked for your computer&#39;s password.</p><div class="custom-container danger"><p class="custom-container-title">NOTE</p><p>The cursor will not move or change as you type the password. If you mistype, hit <code>return</code> and try again.</p></div><p><strong>5. Enter the following command into the Terminal window and hit <code>return</code>.</strong></p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>valet trust
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>You will be asked for your computer&#39;s password.</p><div class="custom-container danger"><p class="custom-container-title">NOTE</p><p>The cursor will not move or change as you type the password. If you mistype, hit <code>return</code> and try again.</p></div><p><strong>4. Enter the following commands into the Terminal window and hit <code>return</code>.</strong></p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">mkdir</span> ~/Sites
<span class="token builtin class-name">cd</span> ~/Sites
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p><strong>5. Enter the following command into the Terminal window and hit <code>return</code>.</strong></p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>valet park
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p><strong>6. Close the Terminal window</strong></p>`,17),o=[i];function r(d,l){return n(),a("div",null,o)}const u=e(t,[["render",r],["__file","valet.html.vue"]]);export{u as default};
