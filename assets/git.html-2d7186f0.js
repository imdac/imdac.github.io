import{_ as n,p as e,q as a,a3 as s}from"./framework-dd056dd6.js";const o={},i=s(`<h1 id="installing-git-on-macos" tabindex="-1"><a class="header-anchor" href="#installing-git-on-macos" aria-hidden="true">#</a> Installing Git on macOS</h1><p><strong>1. Open a new Terminal window</strong></p><p><strong>2. Enter the following command into the Terminal window and hit <code>return</code></strong></p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>brew <span class="token function">install</span> <span class="token function">git</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p><strong>3. Enter the following command into the Terminal window and hit <code>return</code></strong></p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token builtin class-name">echo</span> <span class="token string">&quot;export PATH=/usr/local/bin:<span class="token environment constant">$PATH</span>&quot;</span> <span class="token operator">&gt;&gt;</span> ~/.zshrc
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p><strong>4. Close the Terminal Window</strong></p><p><strong>5. Open a new Terminal window</strong></p><p><strong>6. Enter the following command into the Terminal window replacing <em>Your Name</em> and hit <code>return</code></strong></p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">git</span> config <span class="token parameter variable">--global</span> user.name <span class="token string">&quot;Your Name&quot;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p><strong>7. Enter the following command into the Terminal window replacing <em>youremailaddress</em> and hit <code>return</code></strong></p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">git</span> config <span class="token parameter variable">--global</span> user.email <span class="token string">&quot;youremailaddress@algonquinlive.com&quot;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p><strong>8. Enter the following command into the Terminal window and hit <code>return</code></strong></p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">git</span> config <span class="token parameter variable">--global</span> color.ui auto
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p><strong>9. Close the Terminal window</strong></p>`,15),t=[i];function r(l,d){return e(),a("div",null,t)}const p=n(o,[["render",r],["__file","git.html.vue"]]);export{p as default};
