import{_ as n,p as e,q as s,a3 as a}from"./framework-dd056dd6.js";const t={},o=a(`<h1 id="installing-mysql-for-macos" tabindex="-1"><a class="header-anchor" href="#installing-mysql-for-macos" aria-hidden="true">#</a> Installing MySQL for macOS</h1><p><strong>1. Open a new Terminal window</strong></p><p><strong>2. Enter the following command into the Terminal window and hit <code>return</code>.</strong></p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>brew <span class="token function">install</span> mysql
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p><strong>3. Enter the following command into the Terminal window and hit <code>return</code>.</strong></p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>brew services start mysql
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p><strong>4. Enter the following command into the Terminal window and hit <code>return</code>.</strong></p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>mysql <span class="token parameter variable">-u</span> root
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p><strong>5. Enter the following command into the Terminal window and hit <code>return</code>.</strong></p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>ALTER <span class="token environment constant">USER</span> <span class="token string">&#39;root&#39;</span>@<span class="token string">&#39;localhost&#39;</span> IDENTIFIED WITH caching_sha2_password BY <span class="token string">&#39;root&#39;</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p><strong>6. Close the Terminal window.</strong></p><div class="custom-container danger"><p class="custom-container-title">WARNING</p><p>The following steps are only for those who had trouble completing steps 4 or 5.</p></div><p><strong>1. Open a new Terminal window</strong></p><p><strong>2. Enter the following command into the Terminal window and hit <code>return</code>.</strong></p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>brew services stop mysql
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p><strong>3. Enter the following command into the Terminal window and hit <code>return</code>.</strong></p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>mysqld_safe --skip-grant-tables <span class="token operator">&amp;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><div class="custom-container warning"><p class="custom-container-title">NOTE</p><p>Wait for the process to finish with the message &quot;Starting mysqld daemon with databases...&quot; before continuing.</p></div><p><strong>4. Enter the following command into the Terminal window and hit <code>return</code>.</strong></p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>mysql
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>You should receive the <code>mysql&gt;</code> prompt.</p><p><strong>5. Enter the following command into the Terminal window and hit <code>return</code>.</strong></p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>FLUSH PRIVILEGES<span class="token punctuation">;</span>
ALTER <span class="token environment constant">USER</span> <span class="token string">&#39;root&#39;</span>@<span class="token string">&#39;localhost&#39;</span> IDENTIFIED WITH caching_sha2_password BY <span class="token string">&#39;root&#39;</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p><strong>6. Close the Terminal window.</strong></p>`,24),i=[o];function r(d,l){return e(),s("div",null,i)}const p=n(t,[["render",r],["__file","mysql.html.vue"]]);export{p as default};
